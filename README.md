# Form-Validation
Application for form validation

Uses regular expressions to verify that the user inputs valid data into the forms input fields.

The application is available to be tested at https://pontus-form-validation.herokuapp.com/.
